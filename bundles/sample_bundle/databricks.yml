bundle:
  name: sample_bundle

include:
  - resources/*.y*ml
  - targets/*.y*ml
  - variables.y*ml

sync:
  paths:
    - "../../dbt/"
  exclude:
    - targets

artifacts:
  default:
    type: whl
    build: uv build --all-packages --wheel --out-dir ../dist
    files:
      - source: ../dist/*.whl
